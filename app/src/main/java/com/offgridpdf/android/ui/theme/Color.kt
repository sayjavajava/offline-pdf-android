package com.offgridpdf.android.ui.theme

import androidx.compose.ui.graphics.Color

// Placeholder palette — swap for real brand colors when the product owner
// picks them. Kept deliberately small: two seed colors, Material3's own
// dynamic color/tonal system derives the rest.
val SeedPrimary = Color(0xFF3D5A80)
val SeedSecondary = Color(0xFF98C1D9)
